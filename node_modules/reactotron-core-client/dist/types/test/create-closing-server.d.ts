/**
 * A server which shuts down after somebody connects.
 *
 * @param port The port to listen on
 * @param done Calls after we close.
 */
export declare function createClosingServer(port: number, onDone?: () => void): void;
